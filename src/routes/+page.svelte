<script lang="ts">
	import '$lib';

	const installCode = `npm install XYZ`;
	const importCode = `import "ripple-class";`;
	const buttonCode = `<button class="ripple">Click Me!</button>`;
</script>

<h1>Ripple Class Demo</h1>

<h2>Setup</h2>
<p>Install the package.</p>
<p>Import the ripple library.</p>

<h2>Usage</h2>
<p>You can change the default color by setting the <code>--ripple-color</code> CSS variable.</p>

<style>
</style>
